{
  "name": "steepwell",
  "version": "1.0.0",
  "description": "Steepwell — small-batch loose-leaf tea. Static, dependency-free luxury storefront.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx -y serve -l 4173 .",
    "check": "node --check assets/js/site.js"
  },
  "engines": { "node": ">=18" }
}
