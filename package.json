{
  "name": "functional-elixirs",
  "version": "1.0.0",
  "description": "Functional Elixirs — honey with fresh ginger. Static, dependency-free storefront.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && npx -y serve -l 4173 .",
    "check": "node --check assets/js/site.js"
  },
  "engines": { "node": ">=18" }
}
